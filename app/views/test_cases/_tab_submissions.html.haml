-# Submissions tab — coming in a later commit. Will mirror the
-# test-on-commit instances table, indexed by commit for a single
-# chosen computer (the legacy show's "show_instances" mode,
-# preserved and reskinned).
.rounded-lg.border.border-dashed.border-border.bg-bg-elev.p-10.text-center{ style: "box-shadow: var(--shadow-card-sm);" }
  .text-fg-muted.text-sm.mb-1 Raw test instances for a chosen computer
  .text-fg-subtle{ class: "text-[12px]" } Coming in a later commit.
