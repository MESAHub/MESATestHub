-# One row in the "Available fields" reference panel on
-# test_instances#search. Two locals: `field` (mono identifier)
-# and `desc` (plain prose).
.grid.gap-x-3{ class: "grid-cols-[auto_1fr]" }
  %dt.font-mono.font-semibold.text-fg.self-start= field
  %dd.text-fg-muted= desc
