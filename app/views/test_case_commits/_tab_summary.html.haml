-# Summary tab — the instances panel. Thin wrapper so the
-# tabs_controller's `data-tab="summary"` panel iteration finds a
-# `_tab_summary` partial alongside `_tab_logs`. The actual
-# content (toolbar + icon legend + table) lives in
-# `_instances_panel`.
= render "instances_panel"
